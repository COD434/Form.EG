<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link rel="stylesheet" href="/login.css">
</head>
<body>
    <div class="auth-form">

        <h1>Login</h1>
	<div class="planet">
	<svg class="earth" viewBox="0 0 512 512" xmlns="http://www.w3.org/2000/svg">
    <circle cx="256" cy="256" r="256" fill="#1e90ff" /> 
    <path d="M200,150 l40,20 l20,30 l-10,20 l-30,-10 l-20,20 l-30,-10 l-10,-30 l30,-40 z" fill="#2ecc71" />
    <path d="M300,250 l20,10 l20,30 l-10,20 l-30,0 l-10,-20 l-10,-30 z" fill="#2ecc71" />
    <path d="M120,280 l30,20 l10,10 l-10,10 l-30,0 l-10,-10 l10,-20 z" fill="#2ecc71" />
  </svg>
	</div>
        <form action="/login" method="POST">
            <div class="form-group">
                <label for="username">Username</label>
                <input type="text" 
                       id="username" 
                       name="username" 
                       class="<%= (typeof errors !== 'undefined' && errors.some(e => e.param === 'username')) ? 'is-invalid' : '' %>"
                       required>
                <% if (typeof errors !== 'undefined') { %>
                    <% const usernameError = errors.find(e => e.param === 'username'); %>
                    <% if (usernameError) { %>
                        <div class="invalid-feedback"><%= usernameError.msg %></div>
                    <% } %>
                <% } %>
            </div>
            
            <div class="form-group">
                <label for="email">Email</label>
                <input type="email" 
                       id="email" 
                       name="email" 
                       class="<%= (typeof errors !== 'undefined' && errors.some(e => e.param === 'email')) ? 'is-invalid' : '' %>"
                       required>
                <% if (typeof errors !== 'undefined') { %>
                    <% const emailError = errors.find(e => e.param === 'email'); %>
                    <% if (emailError) { %>
                        <div class="invalid-feedback"><%= emailError.msg %></div>
                    <% } %>
                <% } %>
            </div>
            
            <div class="form-group">
                <label for="password">Password</label>
                <input type="password" 
                       id="password" 
                       name="password" 
                       class="<%= (typeof errors !== 'undefined' && errors.some(e => e.param === 'password')) ? 'is-invalid' : '' %>"
                       required>
                <% if (typeof errors !== 'undefined') { %>
                    <% const passwordError = errors.find(e => e.param === 'password'); %>
                    <% if (passwordError) { %>
                        <div class="invalid-feedback"><%= passwordError.msg %></div>
                    <% } %>
                <% } %>
            </div>
            
            <button type="submit" class="auth-button">Login</button>
        </form>
        
        <p class="auth-link">Don't have an account? <a href="/register">Register here</a></p>
        <p>Dont rememmber? <a href="/forgotPass">Forgot password</a></p>   
    </div>
</body>
</html>
